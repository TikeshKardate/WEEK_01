// This is the main class named Miles
public class Miles { 

    // Main method is the entry point of the program
    public static void main (String[] args) { 
	
	    // Converts 10.8 kilometers to miles (1 km = 1.6 miles)
        double miles = (10.8 * 1.6);
        
		// Prints the converted distance to the console
        System.out.println("The distance " + 10.8 + " km in miles is " + miles); 
    }
}
